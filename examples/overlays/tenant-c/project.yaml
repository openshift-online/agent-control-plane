kind: Project
name: tenant-c
description: OIDC-authenticated tenant demonstrating browser-based login.
prompt: |
  This tenant demonstrates OIDC authentication with Keycloak. Use this tenant
  to test browser-based login flows and role-based access control.
labels:
  env: dev
  auth: oidc
