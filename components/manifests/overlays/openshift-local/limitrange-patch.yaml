apiVersion: v1
kind: LimitRange
metadata:
  name: ambient-default-limits
spec:
  limits:
  - type: Container
    defaultRequest:
      cpu: 50m
      memory: 128Mi
    default:
      cpu: "1"
      memory: 2Gi
