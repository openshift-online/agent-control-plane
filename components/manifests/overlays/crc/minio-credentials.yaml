apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  labels:
    app: minio
type: Opaque
stringData:
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "minioadmin"
  MINIO_ENDPOINT: "http://minio:9000"
  MINIO_BUCKET: "ambient-artifacts"
