---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ambient-api-server
  labels:
    app: ambient-api-server

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ambient-api-server
  labels:
    app: ambient-api-server
    component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ambient-api-server
      component: api
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ambient-api-server
        component: api
    spec:
      serviceAccountName: ambient-api-server
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: migration
          image: quay.io/ambient_code/acp_api_server:latest
          imagePullPolicy: Always
          command:
            - /usr/local/bin/ambient-api-server
            - migrate
            - --db-host-file=/secrets/db/db.host
            - --db-port-file=/secrets/db/db.port
            - --db-user-file=/secrets/db/db.user
            - --db-password-file=/secrets/db/db.password
            - --db-name-file=/secrets/db/db.name
            - --alsologtostderr
            - -v=4
          volumeMounts:
            - name: db-secrets
              mountPath: /secrets/db
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      containers:
        - name: api-server
          image: quay.io/ambient_code/acp_api_server:latest
          imagePullPolicy: Always
          command:
            - /usr/local/bin/ambient-api-server
            - serve
            - --db-host-file=/secrets/db/db.host
            - --db-port-file=/secrets/db/db.port
            - --db-user-file=/secrets/db/db.user
            - --db-password-file=/secrets/db/db.password
            - --db-name-file=/secrets/db/db.name
            - --enable-jwt=true
            - --enable-authz=true
            - --jwk-cert-file=/configs/authentication/jwks.json
            - --enable-https=true
            - --https-cert-file=/etc/tls/tls.crt
            - --https-key-file=/etc/tls/tls.key
            - --enable-tls=true
            - --tls-cert-file=/etc/tls/tls.crt
            - --tls-key-file=/etc/tls/tls.key
            - --tls-min-version=1.2
            - --tls-auto-detect-kubernetes=false
            - --enable-health-check-https=true
            - --api-server-bindaddress=:8000
            - --metrics-server-bindaddress=:4433
            - --health-check-server-bindaddress=:4434
            # SSL disabled: PostgreSQL is in-cluster (same namespace, pod network only).
            # Switch to --db-sslmode=require if using an external database.
            - --db-sslmode=disable
            - --db-max-open-connections=50
            - --enable-db-debug=false
            - --enable-metrics-https=false
            - --http-read-timeout=5s
            - --http-write-timeout=30s
            - --cors-allowed-origins=*
            - --cors-allowed-headers=X-Ambient-Project
            - --enable-grpc=true
            - --grpc-server-bindaddress=:9000
            - --grpc-enable-tls=true
            - --grpc-tls-cert-file=/etc/tls/tls.crt
            - --grpc-tls-key-file=/etc/tls/tls.key
            - --alsologtostderr
            - -v=4
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: AMBIENT_RUNNER_PUBLIC_KEY_FILE
              value: /configs/runner/public.pem
            - name: CONTROL_PLANE_URL
              value: https://ambient-control-plane.$(POD_NAMESPACE).svc:8080
            - name: AMBIENT_ENV
              value: development
            - name: GRPC_SERVICE_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: ambient-api-server
                  key: clientId
                  optional: true
          ports:
            - name: api
              containerPort: 8000
              protocol: TCP
            - name: metrics
              containerPort: 4433
              protocol: TCP
            - name: health
              containerPort: 4434
              protocol: TCP
            - name: grpc
              containerPort: 9000
              protocol: TCP
          volumeMounts:
            - name: db-secrets
              mountPath: /secrets/db
            - name: app-secrets
              mountPath: /secrets/service
            - name: auth-config
              mountPath: /configs/authentication
            - name: runner-public-key
              mountPath: /configs/runner
              readOnly: true
            - name: tls-certs
              mountPath: /etc/tls
              readOnly: true
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /api/ambient
              port: 8000
              scheme: HTTPS
            initialDelaySeconds: 15
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthcheck
              port: 4434
              scheme: HTTPS
              httpHeaders:
                - name: User-Agent
                  value: Probe
            initialDelaySeconds: 20
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: tls-certs
          secret:
            secretName: ambient-api-server-tls
        - name: runner-public-key
          secret:
            secretName: ambient-cp-token-keypair
            items:
              - key: public.pem
                path: public.pem
        - name: db-secrets
          secret:
            secretName: ambient-api-server-db
        - name: app-secrets
          secret:
            secretName: ambient-api-server
        - name: auth-config
          configMap:
            name: ambient-api-server-auth

---
apiVersion: v1
kind: Service
metadata:
  name: ambient-api-server
  labels:
    app: ambient-api-server
    component: api
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: ambient-api-server-tls
    description: Exposes the ambient-api-server REST API
spec:
  selector:
    app: ambient-api-server
    component: api
  ports:
    - name: api
      port: 8000
      targetPort: 8000
      protocol: TCP
    - name: grpc
      port: 9000
      targetPort: 9000
      protocol: TCP
    - name: metrics
      port: 4433
      targetPort: 4433
      protocol: TCP
    - name: health
      port: 4434
      targetPort: 4434
      protocol: TCP
